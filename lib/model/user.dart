class User {
  String firstName, lastName, city, street, zipcode;
  User(
      {required this.firstName,
      required this.lastName,
      required this.city,
      required this.street,
      required this.zipcode});
}
